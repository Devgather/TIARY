aws:
  access-key: ${AWS_ACCESS_KEY:aws-access-key}
  secret-key: ${AWS_SECRET_KEY:aws-secret-key}

  s3:
    region: ap-northeast-2
    bucket: ${AWS_S3_BUCKET:aws-s3-bucket}

  storage:
    url: ${AWS_STORAGE_URL:https://example.com/}