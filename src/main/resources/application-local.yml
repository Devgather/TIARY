spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:tiary;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa

  sql:
    init:
      schema-locations: classpath*:database/schema.sql

  jpa:
    open-in-view: false
    show-sql: true

    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: true
